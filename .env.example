# ==============================================================
# AEROPUERTOS GLOBAL - Variables de Entorno (PLANTILLA)
# ==============================================================
# INSTRUCCIONES:
# 1. Copiar este archivo a '.env' (sin '.example')
# 2. Completar los valores reales (sin comillas)
# 3. NUNCA commitear el archivo .env al repositorio
# ==============================================================

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# DJANGO - Configuración del Framework
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Clave secreta de Django (CRÍTICO: generar una única y segura)
# Para generar: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DJANGO_SECRET_KEY=tu-clave-secreta-super-larga-y-aleatoria-aqui

# Modo de depuración (True solo en desarrollo local, False en producción)
DEBUG=True

# Hosts permitidos (separados por comas, sin espacios)
# Ejemplos: localhost,127.0.0.1 (desarrollo)
#           empresa.azurewebsites.net,www.empresa.com (producción)
ALLOWED_HOSTS=localhost,127.0.0.1

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# BASE DE DATOS - PostgreSQL + PostGIS
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Opción 1: Configuración detallada (desarrollo local)
DB_NAME=aeropuertos_db
DB_USER=postgres
DB_PASSWORD=tu_password_postgresql
DB_HOST=localhost
DB_PORT=5432

# Opción 2: URL completa de conexión (producción en Render, Azure, etc.)
# Si DATABASE_URL está definida, las variables individuales de arriba se ignoran
# Formato: postgresql://usuario:password@host:puerto/nombre_bd
# DATABASE_URL=postgresql://user:pass@host.render.com:5432/aeropuertos_db

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# SERVICIOS EXTERNOS - API Keys
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# MapTiler API Key (para el mapa base)
# Obtener en: https://cloud.maptiler.com/account/keys/
# Formato: pk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
MAPTILER_API_KEY=tu_clave_maptiler_aqui

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# RENDER.COM (si se despliega en Render)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Hostname externo de Render (se configura automáticamente)
# RENDER_EXTERNAL_HOSTNAME=aeropuertos-web.onrender.com

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# CONFIGURACIÓN ADICIONAL (Opcional)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# Zona horaria (por defecto: America/Mexico_City)
# TZ=America/Mexico_City

# Idioma (por defecto: es-mx)
# LANGUAGE_CODE=es-mx

# Email (si se requiere envío de correos)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=tu_email@empresa.com
# EMAIL_HOST_PASSWORD=tu_password_email
# EMAIL_USE_TLS=True

# Logging (nivel de detalle: DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# EJEMPLO DE VALORES COMPLETADOS (DESARROLLO LOCAL)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Descomenta y modifica las siguientes líneas:

# DJANGO_SECRET_KEY=django-insecure-abc123xyz789-ejemplo-no-usar
# DEBUG=True
# ALLOWED_HOSTS=localhost,127.0.0.1

# DB_NAME=aeropuertos_db
# DB_USER=postgres
# DB_PASSWORD=mipassword123
# DB_HOST=localhost
# DB_PORT=5432

# MAPTILER_API_KEY=pk_abc123xyz789ejemplo

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# EJEMPLO DE VALORES COMPLETADOS (PRODUCCIÓN)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# DJANGO_SECRET_KEY=django-insecure-CAMBIAR-POR-CLAVE-REAL-SUPER-LARGA
# DEBUG=False
# ALLOWED_HOSTS=aeropuertos.empresa.com,www.aeropuertos.empresa.com

# DATABASE_URL=postgresql://user:pass@db.render.com:5432/aeropuertos_xyz

# MAPTILER_API_KEY=pk_REAL_KEY_AQUI

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# NOTAS DE SEGURIDAD
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ⚠️ NO compartir este archivo con valores reales
# ⚠️ NO commitear a Git (verificar .gitignore)
# ⚠️ Usar diferentes claves entre desarrollo y producción
# ⚠️ Rotar credenciales periódicamente
# ✅ Usar gestores de secretos corporativos (Azure Key Vault, AWS Secrets Manager)
